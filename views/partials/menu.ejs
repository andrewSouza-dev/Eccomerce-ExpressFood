<% const u = typeof user !== 'undefined' ? user : null; %>

<%- include('head', { title: 'FoodExpress 🍽️' }) %>

<nav class="main-nav">
  <% if (u) { %>
    <% if (u.isAdmin) { %>

      <!-- Admin vê apenas Admin + Sair -->
      <a href="/admin" class="nav-link">Admin</a>
      <a href="/auth/logout" class="nav-link">Sair</a>
    <% } else { %>

      <!-- Cliente vê Home + Carrinho + Sair -->
      <a href="/home" class="nav-link">Inicio</a>
      <a href="/cart" class="nav-link">Carrinho</a>
      <a href="/auth/logout" class="nav-link">Sair</a>
    <% } %>
  <% } else { %>

    <!-- Não logado vê Login + Cadastro -->
    <a href="/auth/login" class="nav-link">Login</a>
    <a href="/auth/cadastro" class="nav-link">Cadastro</a>
    <a href="/" class="nav-link">Sair</a>
  <% } %>
</nav>
